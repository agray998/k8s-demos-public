kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: myclaim
spec:
  storageClassName: "foobar"
  # volumeName: nfs-pv
  accessModes:
  - ReadWriteMany
  resources:
    requests: 
      storage: 1Gi